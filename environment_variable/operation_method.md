## 環境変数の設定・修正方法
```sh
# 環境変数の設定画面を開く
% vim ~/.zshrc

# 編集する
INSERTキーを押す。過去の記述は削除しないこと！

# INSERTモードの終了
escキーを押す。

# 環境変数の設定の終了
# 保存
% :w          # writeの意

# 保存して終了
% :wq         # write & quit

# 保存しないで終了
% :q          # quit

# 強制終了・強制保存
末尾に"!"をつけると、強制保存（終了）となる

# 設定を反映させる
% source ~/.zshrc
```